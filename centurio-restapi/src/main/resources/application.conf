ktor {
    deployment {
        port = 8080
        port = ${?PORT}
        environment = dev
        environment = ${?KTOR_ENV}
        watch = [  build ]
    }

    application {
        modules = [ fr.hadaly.MainKt.module ]
    }
}
